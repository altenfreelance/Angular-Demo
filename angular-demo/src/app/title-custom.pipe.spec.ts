import { TitleCustomPipe } from './title-custom.pipe';

describe('TitleCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
